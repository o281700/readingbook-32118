<footer>
  <% if user_signed_in? %>
    <div class='user-guide guide-under'>
      <% if controller_name == 'books' && action_name == 'show' && book.user.id == current_user.id %>
        <%= link_to('削除' , book_path(book.id), method: :delete) %>
        <%= link_to '編集', edit_book_path(book.id) %>
      <% end %>
      <%= link_to '新規投稿', new_book_path %>
    </div>
  <% end %>
</footer>